Encapsulation → Protect data by bundling it with methods and controlling access.
Abstraction → Hide complexity and expose only what users need.
Inheritance → Reuse code by creating new classes from existing ones.
Polymorphism → Same method, different behavior depending on the object.